const pathImages = "assets/images/";

const title = Title`
  .title {
    color: var(--title);
    font-size: 2rem;
    letter-spacing: 1.5px;
  }
  ${"Dificuldade"}
  ${"title"}
`;

injectElements(title);
